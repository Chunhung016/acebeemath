# ACEBEE 数学冒险

双击同目录的 `index.html` 即可开始。无需登录、安装、网络或服务器。请保留旁边的 `images` 文件夹；分享游戏时，请一起复制整个游戏文件夹。

## 本次更新

- **动手操作**：一次拖动一个物品。放入后，每个物品都在容器里保持可见，放几个就显示几个，放大的数字徽章同步更新。直接按住容器里的任意物品，可以拖出重新分配；“撤销”回到上一步，“重来”恢复当前题。
- **容器内的物品**：适用于全部容器和场景。花朵的茎伸进花盆的土里，水果摆在篮子或碗里，鱼显示在鱼缸的水中，背包等使用可见口袋。数量较多时，容器区域增高，保持每个物品可见、可数、可拖动。
- **乘法**：组数 × 每组数量。第一题为 3 个背包，每个装 2 个苹果。摆好并检查后，展示 `3 × 2 = 6` 和 `2 + 2 + 2 = 6`，不要求孩子额外输入答案。
- **加法**：将两个来源的物品合并到一个容器。
- **减法**：容器开始装有全部物品，拖出指定数量到“拿走区”。同时核对拿走和剩余的数量；可以拖回纠正。
- **除法**：把全部物品平均分入指定数量的容器。
- **看图列式**：提供试卷风格和彩图切换。首题对应参考图的结构：5 组、每组 4 个，填写 `5 × 4 = 20`。三个空格均需完成。
- **答题训练场**：选择题、数字输入和实际连线。输入框支持键盘；连线的每个答案最多连接一道题。
- **混合大挑战**：十道题结合操作、看图和计算，连续答对有额外星星。默认不计时；设置中可开启每题 60 秒。时间到后仍可继续思考。

## 音乐和设置

右上角音符按钮控制全部声音。音乐在第一次点击或触摸后开始，符合浏览器的音频规则。设置里可关闭背景音乐、调低音量、关闭动画、选择难度或固定场景。浏览器转入后台后声音和挑战计时暂停。

难度和场景从下一题生效。游戏使用原有 PNG 素材：16 种计数物件、12 种容器、9 张背景，以及蜜蜂与奖励图片。默认场景随关卡变化；设置中的场景选择可访问全部组合。

星星、等级和设置保存在当前浏览器。关闭页面后可重新开始一轮；未完成的一题不会恢复。浏览器的隐私模式、清除网站数据或更换浏览器，可能影响本地保存。

## 文件

- `index.html`：新版游戏，CSS、游戏逻辑和合成音乐均包含其中。
- `images/`：保留的原始游戏素材。
- `index.before-enhancement.html`：修改前的 HTML 备份。
- `index.before-visible-objects.html`：改为容器内物品可见之前的版本备份。
- `tests/verify.cjs`：浏览器回归检查，需要 Node.js、Playwright 和 Chrome，仅开发验证使用，玩游戏无需安装。
- `tests/results/report.json`：最近一次验证结果。

触屏验证使用 Chromium 的真实触摸事件模拟，覆盖手机和 iPad 尺寸；仍建议发布前在实际 iPad Safari 上试玩一次。
